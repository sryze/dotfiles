- clean: [~]

- link:
  ~/.bashrc: bashrc
  ~/.gitconfig: gitconfig
  ~/.gitignore: gitignore
  ~/.vim: vim/
  ~/.vimrc: vimrc

- shell:
  - [git submodule update --init --recursive, Installing submodules]
